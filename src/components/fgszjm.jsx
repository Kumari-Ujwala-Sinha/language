{/*import React from "react";

function Footer(props) {

  const posts=[{title:"1anything",titlem:"1ശേഷവും",para:"1dgsb",param:"1ശേഷവും"},
{title:"anything1",titlem:"ശേഷവും1",para:"dgsb1",param:"ശേഷവും1"}] 

  if (props.language === "Malayalam") {
    return( <div>{posts.map(post=>{
      return  <div><p>{post.titlem}</p><p>{post.param}</p></div>
  })}</div>)
      
    
  }
  <Route
  path="/page"
  render={() => (
    <Page
    //pass the props through the route
      username={username}
      password={password}
      email={email}
    />
  )}
  exact={true}
/>
  return( <div>{posts.map(post=>{
    return  <div><p>{post.title}</p><p>{post.para}</p></div>
})}</div>)

}

export default Footer;*/}